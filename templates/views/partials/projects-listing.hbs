{{#ifgt data.posts.length 0}}

<!--||||||||||||||||-->
<!--PROJECTS LISTING-->
<!--||||||||||||||||-->

<div class="container-fluid background dark-grey">
	<div class="row">
		<div class="container projects-container">
			<div class="row">
					{{#each data.featuredCategoryPosts }}
					<!--ORDERED POST-->
						<a href="{{postUrl slug}}" class="project-link">
							<div class="col-sm-6 {{getColAdvanced @index (addLength ../data.posts ../data.featuredCategoryPosts) 0}} project-col">			
								<div class="project-title-container">
									<h3 class="project-title">{{title}}</h3>
								</div>
								{{#if mainImage.exists}}								
									<div class="project-image-container">
										{{#ifCropLeftOvers @index ../data.posts.length}}
											<img class="cld-responsive img-responsive" data-src="{{cloudinaryUrl mainImage width='auto' height=329 q='70' crop='fill' gravity='center'}}"/>
										{{else}}
											<img class="cld-responsive img-responsive" data-src="{{cloudinaryUrl mainImage width='auto' q='70'}}"/>
										{{/ifCropLeftOvers}}
									</div>
								{{/if}}
							</div>	
						</a>
					{{/each}}
					{{#each data.posts }}
					<!--NORMAL NOT ORDERED POST-->
						<a href="{{postUrl slug}}" class="project-link">
							<div class="col-sm-6 {{getColAdvanced @index (addLength ../data.posts ../data.featuredCategoryPosts) (getLength ../data.featuredCategoryPosts)}} project-col">			
								<div class="project-title-container">
									<h3 class="project-title">{{title}}</h3>
								</div>
								{{#if mainImage.exists}}								
									<div class="project-image-container">
										{{#ifCropLeftOvers @index ../data.posts.length}}
											<img class="cld-responsive img-responsive" data-src="{{cloudinaryUrl mainImage width='auto' height=329 q='70' crop='fill' gravity='center'}}"/>
										{{else}}
											<img class="cld-responsive img-responsive" data-src="{{cloudinaryUrl mainImage width='auto' q='70'}}"/>
										{{/ifCropLeftOvers}}
									</div>
								{{/if}}
							</div>	
						</a>
					{{/each}}
			</div>
		</div>
	</div>
</div>
{{/ifgt}}